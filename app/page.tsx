import ProtoShell from "@/components/v3/ProtoShell";
import "./proto.css";

// The game lives at the root: a shareable link should be the domain and nothing else.
// The horizontal V2 is archived at /v2 as a comparison point.
export default function HomePage() {
  return <ProtoShell />;
}
